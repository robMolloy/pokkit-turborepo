---
sidebar_position: 2
---

# Spec

## Goal

Pokkit Db Config Sync is a PocketBase plugin that makes it easier to manage the configuration of a PocketBase database. It does this by introducing a small set of config collections and a reusable rule syntax that other collections can reference.
